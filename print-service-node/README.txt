INSTRUCCIONES PARA CREAR EL .EXE

1. Asegurate de tener Node.js instalado.
2. Instala 'pkg' globalmente (una sola vez):
   npm install -g pkg

3. Desde la carpeta del proyecto, corre:
   npm install
   pkg .

Esto generará un archivo ejecutable como:
   print-service-node.exe